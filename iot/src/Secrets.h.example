// iot/src/Secrets.h.example — template. Copy to Secrets.h (git-ignored) and
// fill in YOUR 2.4 GHz WiFi so the ESP32 can join it in station mode.
#ifndef SECRETS_H
#define SECRETS_H

#define WIFI_STA_SSID       "YOUR_WIFI_NAME"
#define WIFI_STA_PASSWORD   "YOUR_WIFI_PASSWORD"

#endif // SECRETS_H
